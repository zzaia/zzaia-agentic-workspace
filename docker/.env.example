# Required
WORKSPACE_NAME=my-org
SSH_PUBLIC_KEY=ssh-ed25519 AAAA...
GPU_ENABLED=false

# Profiles (optional): vscode, jupyter, devcontainer, tunnel
DEPLOY_PROFILES=

# Port configuration (optional)
VAULT_PORT=8200
SSH_PORT=2222
VSCODE_PORT=8080
ASPIRE_DASHBOARD_PORT=18888
JUPYTER_PORT=8888

# Vault bootstrap
VAULT_ROOT_TOKEN=           # Strong token for Vault initialization

# Bitwarden Secrets Manager (consumed at deploy time, not persisted)
BWS_ACCESS_TOKEN=

# Database default password (optional)
ADMIN_PASSWORD=
