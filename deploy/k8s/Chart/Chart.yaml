apiVersion: v2
name: zzaia-agentic-workspace
description: Agentic development workspace (workspace-server, IDE sidecars, MCP fleet, DinD, Portainer) ported from docker/docker-compose.yml
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - agentic
  - workspace
  - mcp
  - developer-environment
  - dind
maintainers:
  - name: ZZAIA Team
  - name: Platform Team
    email: platform@zzaia.com
# NOTE: This chart deploys workloads + ClusterSecretStore for Bitwarden Secrets Manager.
# Pre-installed cluster components: External Secrets Operator, Kong, PriorityClass,
# RuntimeClass (nvidia, if available).
